import { StyleSheet } from 'react-native';

export const movieVisual_styles = StyleSheet.create({
  title: {
    fontSize: 25,
    color: 'black',
    textAlign: 'center',
    fontWeight: 'bold',
  },
});
