import { StyleSheet } from 'react-native';

export const muvieVisual_styles = StyleSheet.create({
  title: {
    fontSize: 25,
    color: 'black',
    textAlign: 'center',
    fontWeight: 'bold',
  },
});
