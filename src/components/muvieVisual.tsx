import React from 'react';
import { View, Text } from 'react-native';

const MuvieVisual = ({ muvies }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default MuvieVisual;
